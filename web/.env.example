# API Configuration

# For local development (when running backend on port 3000)
VITE_API_BASE_URL=http://localhost:3000/api

# For Docker deployment, use relative path (nginx will proxy)
# VITE_API_BASE_URL=/api
