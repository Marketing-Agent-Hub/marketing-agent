# Deployment Scripts

- `deploy.sh`: Main deployment entrypoint (tag-aware Docker deployment).
- `webhook-deploy.sh`: Webhook-triggered deploy helper.
- `webhook.conf.example`: Example webhook service config for VPS.

Expected repository path on VPS: `/opt/MarketingAgent`.
