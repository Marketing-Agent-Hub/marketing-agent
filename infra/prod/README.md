# Production Infrastructure

This environment contains production-ready infrastructure assets.

- `network/`: networking and ingress (Nginx, LB, DNS-related files)
- `compute/`: deployment/runtime scripts and service orchestration
- `data/`: data layer configs (DB, object storage, retention)
- `observability/`: metrics, logging, tracing, alerting configs
